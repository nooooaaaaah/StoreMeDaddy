<EditForm class="mt-9 text-lg leading-relaxed justify-evenly bg-slate-900 rounded-lg inline-block w-[450px] h-52"
    Model="@notificationsettings" OnValidSubmit="@HandleValidSubmit">
    <h2 class="text-2xl">Notifications</h2>
</EditForm>
<button class="rounded-md bg-indigo-900 w-24 h-12" type="submit">Submit</button>

@code
{
    private NotificationSettingsModel notificationsettings = new();

    private class NotificationSettingsModel
    {
        public bool Email { get; set; }
        public bool Push { get; set; }
        public bool Text { get; set; }
    }
    private void HandleValidSubmit()
    {
        // Handle form submission
    }
}